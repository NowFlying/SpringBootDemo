spring:
  application:
    name: admin-client
  boot:
    admin:
      client:
        url: http://localhost:9080
server:
  port: 9081

management:
  endpoints:
    web:
      exposure:
        include: '*'
  endpoint:
    health:
      show-details: ALWAYS
